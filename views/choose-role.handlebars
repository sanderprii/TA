<div class="container">
"<h2>Choose Your Role</h2>
<div class="d-flex flex-column">
    {{#each roles}}
        <a href="{{this.url}}" class="btn btn-home mb-3 btn-tr w-100">{{this.label}}</a>
    {{/each}}
</div>
</div>
